import type { UniversalBoonDefinition } from '../../types/nineStrain';
import {
  FAULTLINE_CORE_IDS,
  FAULTLINE_MANIFESTATION_ID,
  FAULTLINE_SUPPORT_IDS,
  FAULTLINE_VERDICT_ID,
} from '../../types/faultline';

const PROC = { blockSelfRecursion: true } as const;
const WAVE = 3 as const;

export const FAULTLINE_DEFINITIONS: readonly UniversalBoonDefinition[] = [
  {
    id: FAULTLINE_CORE_IDS.STRESS_PATTERN,
    displayName: 'Stress Pattern',
    strainId: 'FAULTLINE',
    role: 'CORE',
    imprint: 'ARMAMENT',
    acquisitionWave: WAVE,
    prerequisites: {},
    exclusions: [],
    triggerGranularity: 'ONCE_PER_PLAYER_TURN',
    eventSubscriptions: ['ROOT_ACTION_RESOLVED'],
    effectPrimitives: [{ kind: 'FAULTLINE_STRESS_PATTERN' }],
    adapterNeeds: [],
    procGuards: PROC,
    bossTranslation: 'BOUNDED',
    previewModel: 'COMBAT_DELTA',
    persistenceSchema: 'faultline.core.v1',
    refinementHooks: [],
    playerFacingSummary:
      'Armament Core. First weapon or basic root each player turn: 2 Fault on the locked primary, 1 Fault on every other natively affected enemy. Threshold 4 Ruptures.',
  },
  {
    id: FAULTLINE_CORE_IDS.APPLIED_FRACTURE,
    displayName: 'Applied Fracture',
    strainId: 'FAULTLINE',
    role: 'CORE',
    imprint: 'DISCIPLINE',
    acquisitionWave: WAVE,
    prerequisites: {},
    exclusions: [],
    triggerGranularity: 'ONCE_PER_PLAYER_TURN',
    eventSubscriptions: ['ROOT_ACTION_RESOLVED'],
    effectPrimitives: [{ kind: 'FAULTLINE_APPLIED_FRACTURE' }],
    adapterNeeds: [],
    procGuards: PROC,
    bossTranslation: 'BOUNDED',
    previewModel: 'COMBAT_DELTA',
    persistenceSchema: 'faultline.core.v1',
    refinementHooks: [],
    playerFacingSummary:
      'Discipline Core. First Technique or Flex root each player turn applies min(3, 1 + floor(actual paid AP / 2)) Fault to the locked primary or a revealed hostile fallback.',
  },
  {
    id: FAULTLINE_CORE_IDS.COUNTERPRESSURE,
    displayName: 'Counterpressure',
    strainId: 'FAULTLINE',
    role: 'CORE',
    imprint: 'INSTINCT',
    acquisitionWave: WAVE,
    prerequisites: {},
    exclusions: [],
    triggerGranularity: 'ONCE_PER_COMBAT_CYCLE',
    eventSubscriptions: ['INSTINCT_RESOLVED'],
    effectPrimitives: [{ kind: 'FAULTLINE_COUNTERPRESSURE' }],
    adapterNeeds: ['INSTINCT'],
    procGuards: PROC,
    bossTranslation: 'BOUNDED',
    previewModel: 'COMBAT_DELTA',
    persistenceSchema: 'faultline.core.v1',
    refinementHooks: [],
    playerFacingSummary:
      'Instinct Core. First successful Instinct each combat cycle: Standard 1 / Clean 2 / Perfect 3 Fault. Failed Instincts apply none and do not consume the guard.',
  },
  {
    id: FAULTLINE_CORE_IDS.LOAD_LIMIT,
    displayName: 'Load Limit',
    strainId: 'FAULTLINE',
    role: 'CORE',
    imprint: 'CURRENT',
    acquisitionWave: WAVE,
    prerequisites: {},
    exclusions: [],
    triggerGranularity: 'ONCE_PER_PLAYER_TURN',
    eventSubscriptions: ['CURRENT_GAINED', 'CURRENT_SPENT', 'CURRENT_THRESHOLD_ENTERED', 'CURRENT_CYCLE_COMPLETED', 'CURRENT_EMPTIED'],
    effectPrimitives: [{ kind: 'FAULTLINE_LOAD_LIMIT' }],
    adapterNeeds: ['CURRENT'],
    procGuards: PROC,
    bossTranslation: 'BOUNDED',
    previewModel: 'COMBAT_DELTA',
    persistenceSchema: 'faultline.core.v1',
    refinementHooks: [],
    playerFacingSummary:
      'Current Core. First qualifying Current root each player turn: 1 Fault on an ordinary signal, 2 Fault on a major threshold or completed cycle. Both on one root apply only the major value.',
  },
  {
    id: FAULTLINE_SUPPORT_IDS.HAIRLINE_CASCADE,
    displayName: 'Hairline Cascade',
    strainId: 'FAULTLINE',
    role: 'SUPPORT',
    acquisitionWave: WAVE,
    prerequisites: { producerRoles: ['CORE'] },
    exclusions: [],
    triggerGranularity: 'ONCE_PER_PLAYER_TURN',
    eventSubscriptions: ['RUPTURE_RESOLVED'],
    effectPrimitives: [{ kind: 'FAULTLINE_HAIRLINE_CASCADE' }],
    adapterNeeds: [],
    procGuards: PROC,
    bossTranslation: 'BOUNDED',
    previewModel: 'COMBAT_DELTA',
    persistenceSchema: 'faultline.support.v1',
    refinementHooks: [],
    playerFacingSummary:
      'Support. First Rupture each player turn applies 1 Fault to every other enemy the same root directly affected. Transferred Fault may Rupture; that Rupture cannot cascade again.',
  },
  {
    id: FAULTLINE_SUPPORT_IDS.RESIDUAL_STRESS,
    displayName: 'Residual Stress',
    strainId: 'FAULTLINE',
    role: 'SUPPORT',
    acquisitionWave: WAVE,
    prerequisites: { producerRoles: ['CORE'] },
    exclusions: [],
    triggerGranularity: 'ONCE_PER_ROOT_ACTION',
    eventSubscriptions: ['RUPTURE_RESOLVED'],
    effectPrimitives: [{ kind: 'FAULTLINE_RESIDUAL_STRESS' }],
    adapterNeeds: [],
    procGuards: PROC,
    bossTranslation: 'NONE',
    previewModel: 'COMBAT_DELTA',
    persistenceSchema: 'faultline.support.v1',
    refinementHooks: [],
    playerFacingSummary:
      'Support. After a normal Core-threshold Rupture, a surviving target retains exactly 1 Fault. Verdict, transfer, and bonus Ruptures do not leave Residual Stress.',
  },
  {
    id: FAULTLINE_MANIFESTATION_ID,
    displayName: 'Chain Failure',
    strainId: 'FAULTLINE',
    role: 'MANIFESTATION',
    acquisitionWave: WAVE,
    prerequisites: {
      minDepth: 2,
      minOwnedFromStrain: 2,
      requireCorePlusExtraFromStrain: true,
      producerRoles: ['CORE'],
    },
    exclusions: [],
    triggerGranularity: 'ONCE_PER_PLAYER_TURN',
    eventSubscriptions: ['RUPTURE_RESOLVED'],
    effectPrimitives: [{ kind: 'FAULTLINE_CHAIN_FAILURE' }],
    adapterNeeds: [],
    procGuards: PROC,
    bossTranslation: 'BOUNDED',
    previewModel: 'COMBAT_DELTA',
    persistenceSchema: 'faultline.manifestation.v1',
    refinementHooks: [],
    playerFacingSummary:
      'Manifestation. Depth 2+. First normal Core Rupture each player turn applies 2 Fault to the other living enemy with the highest Fault, or 1 Fault to a revealed fallback.',
  },
  {
    id: FAULTLINE_VERDICT_ID,
    displayName: 'Terminal Failure',
    strainId: 'FAULTLINE',
    role: 'VERDICT',
    acquisitionWave: WAVE,
    prerequisites: {},
    exclusions: [],
    triggerGranularity: 'ONCE_PER_ROOT_ACTION',
    eventSubscriptions: ['ULTIMATE_COMMITTED', 'ULTIMATE_RESOLVED'],
    effectPrimitives: [{ kind: 'FAULTLINE_TERMINAL_FAILURE' }],
    adapterNeeds: [],
    procGuards: PROC,
    bossTranslation: 'BOUNDED',
    previewModel: 'COMBAT_DELTA',
    persistenceSchema: 'faultline.verdict.v1',
    refinementHooks: [],
    playerFacingSummary:
      'Verdict. Premium source. A committed equipped ultimate translates one Rupture per locked target, then surviving targets gain 2 Fault. Does not chain or leave Residual Stress.',
  },
];

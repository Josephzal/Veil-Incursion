import type { UniversalBoonDefinition } from '../../types/nineStrain';
import {
  SHARDSKIN_CORE_IDS,
  SHARDSKIN_MANIFESTATION_ID,
  SHARDSKIN_SUPPORT_IDS,
  SHARDSKIN_VERDICT_ID,
} from '../../types/shardskin';

const PROC = { blockSelfRecursion: true } as const;
const WAVE = 4 as const;

export const SHARDSKIN_DEFINITIONS: readonly UniversalBoonDefinition[] = [
  {
    id: SHARDSKIN_CORE_IDS.CRYSTAL_EDGE,
    displayName: 'Crystal Edge',
    strainId: 'SHARDSKIN',
    role: 'CORE',
    imprint: 'ARMAMENT',
    acquisitionWave: WAVE,
    prerequisites: {},
    exclusions: [],
    triggerGranularity: 'ONCE_PER_PLAYER_TURN',
    eventSubscriptions: ['ROOT_ACTION_RESOLVED'],
    effectPrimitives: [{ kind: 'SHARDSKIN_CRYSTAL_EDGE' }],
    adapterNeeds: [],
    procGuards: PROC,
    bossTranslation: 'NONE',
    previewModel: 'COMBAT_DELTA',
    persistenceSchema: 'shardskin.core.v1',
    refinementHooks: [],
    playerFacingSummary:
      'Armament Core. The first non-ultimate weapon or basic root each player turn that deals native direct damage generates Shards equal to 20% of the total native direct damage it actually dealt, capped at 8 per root.',
  },
  {
    id: SHARDSKIN_CORE_IDS.RITUAL_PANE,
    displayName: 'Ritual Pane',
    strainId: 'SHARDSKIN',
    role: 'CORE',
    imprint: 'DISCIPLINE',
    acquisitionWave: WAVE,
    prerequisites: {},
    exclusions: [],
    triggerGranularity: 'ONCE_PER_PLAYER_TURN',
    eventSubscriptions: ['ROOT_ACTION_RESOLVED'],
    effectPrimitives: [{ kind: 'SHARDSKIN_RITUAL_PANE' }],
    adapterNeeds: [],
    procGuards: PROC,
    bossTranslation: 'NONE',
    previewModel: 'COMBAT_DELTA',
    persistenceSchema: 'shardskin.core.v1',
    refinementHooks: [],
    playerFacingSummary:
      'Discipline Core. The first valid paid Technique or Flex each player turn generates 3 Shards plus 2 per actual AP paid, capped at 9, whether or not it deals damage.',
  },
  {
    id: SHARDSKIN_CORE_IDS.PERFECT_FACET,
    displayName: 'Perfect Facet',
    strainId: 'SHARDSKIN',
    role: 'CORE',
    imprint: 'INSTINCT',
    acquisitionWave: WAVE,
    prerequisites: {},
    exclusions: [],
    triggerGranularity: 'ONCE_PER_COMBAT_CYCLE',
    eventSubscriptions: ['INSTINCT_RESOLVED'],
    effectPrimitives: [{ kind: 'SHARDSKIN_PERFECT_FACET' }],
    adapterNeeds: ['INSTINCT'],
    procGuards: PROC,
    bossTranslation: 'NONE',
    previewModel: 'COMBAT_DELTA',
    persistenceSchema: 'shardskin.core.v1',
    refinementHooks: [],
    playerFacingSummary:
      'Instinct Core. The first successful positive Instinct resolution each combat cycle generates Shards by grade: 4 Standard, 7 Clean, 10 Perfect. Failed grants none.',
  },
  {
    id: SHARDSKIN_CORE_IDS.PRESSURE_CRYSTAL,
    displayName: 'Pressure Crystal',
    strainId: 'SHARDSKIN',
    role: 'CORE',
    imprint: 'CURRENT',
    acquisitionWave: WAVE,
    prerequisites: {},
    exclusions: [],
    triggerGranularity: 'ONCE_PER_PLAYER_TURN',
    eventSubscriptions: ['CURRENT_GAINED', 'CURRENT_SPENT', 'CURRENT_THRESHOLD_ENTERED', 'CURRENT_CYCLE_COMPLETED', 'CURRENT_EMPTIED'],
    effectPrimitives: [{ kind: 'SHARDSKIN_PRESSURE_CRYSTAL' }],
    adapterNeeds: ['CURRENT'],
    procGuards: PROC,
    bossTranslation: 'NONE',
    previewModel: 'COMBAT_DELTA',
    persistenceSchema: 'shardskin.core.v1',
    refinementHooks: [],
    playerFacingSummary:
      'Current Core. The first qualifying Current event each player turn generates 4 Shards; a major threshold or completed class cycle instead generates 8. Ultimate-owned refills and preservation events never qualify.',
  },
  {
    id: SHARDSKIN_SUPPORT_IDS.TEMPERED_REMNANT,
    displayName: 'Tempered Remnant',
    strainId: 'SHARDSKIN',
    role: 'SUPPORT',
    acquisitionWave: WAVE,
    prerequisites: { producerRoles: ['CORE'] },
    exclusions: [],
    triggerGranularity: 'ONCE_PER_PLAYER_TURN',
    eventSubscriptions: ['PLAYER_TURN_STARTED'],
    effectPrimitives: [{ kind: 'SHARDSKIN_TEMPERED_REMNANT' }],
    adapterNeeds: [],
    procGuards: PROC,
    bossTranslation: 'NONE',
    previewModel: 'COMBAT_DELTA',
    persistenceSchema: 'shardskin.support.v1',
    refinementHooks: [],
    playerFacingSummary:
      'Support. At the next player-turn conversion, return Edge equal to 50% of the total Shards actually consumed preventing damage since the previous conversion, capped at 8, folded into the normal Edge cap.',
  },
  {
    id: SHARDSKIN_SUPPORT_IDS.SCATTERGLASS,
    displayName: 'Scatterglass',
    strainId: 'SHARDSKIN',
    role: 'SUPPORT',
    acquisitionWave: WAVE,
    prerequisites: { producerRoles: ['CORE'] },
    exclusions: [],
    triggerGranularity: 'ONCE_PER_TARGET_PER_ROOT_ACTION',
    eventSubscriptions: ['ROOT_ACTION_RESOLVED'],
    effectPrimitives: [{ kind: 'SHARDSKIN_SCATTERGLASS' }],
    adapterNeeds: [],
    procGuards: PROC,
    bossTranslation: 'NONE',
    previewModel: 'COMBAT_DELTA',
    persistenceSchema: 'shardskin.support.v1',
    refinementHooks: [],
    playerFacingSummary:
      'Support. When Edge is consumed by a multi-target native root, every other distinct enemy that actually received native direct damage from that root takes Occult damage equal to 50% of the consumed Edge, once per secondary target per root.',
  },
  {
    id: SHARDSKIN_MANIFESTATION_ID,
    displayName: 'Endless Facet',
    strainId: 'SHARDSKIN',
    role: 'MANIFESTATION',
    acquisitionWave: WAVE,
    prerequisites: {
      minDepth: 2,
      minOwnedFromStrain: 2,
      requireCorePlusExtraFromStrain: true,
      producerRoles: ['CORE'],
    },
    exclusions: [],
    triggerGranularity: 'ONCE_PER_PLAYER_TURN',
    eventSubscriptions: ['ROOT_ACTION_RESOLVED', 'ULTIMATE_RESOLVED'],
    effectPrimitives: [{ kind: 'SHARDSKIN_ENDLESS_FACET' }],
    adapterNeeds: [],
    procGuards: PROC,
    bossTranslation: 'NONE',
    previewModel: 'COMBAT_DELTA',
    persistenceSchema: 'shardskin.manifestation.v1',
    refinementHooks: [],
    playerFacingSummary:
      'Manifestation. Depth 2+. After a positive amount of Edge is consumed, reform Shards equal to 50% of the consumed Edge, once per player turn, subject to the global Shard cap. Never triggers from Edge’s own packet or from Scatterglass.',
  },
  {
    id: SHARDSKIN_VERDICT_ID,
    displayName: 'Cathedral Break',
    strainId: 'SHARDSKIN',
    role: 'VERDICT',
    acquisitionWave: WAVE,
    prerequisites: {},
    exclusions: [],
    triggerGranularity: 'ONCE_PER_ROOT_ACTION',
    eventSubscriptions: ['ULTIMATE_COMMITTED', 'ULTIMATE_RESOLVED'],
    effectPrimitives: [{ kind: 'SHARDSKIN_CATHEDRAL_BREAK' }],
    adapterNeeds: [],
    procGuards: PROC,
    bossTranslation: 'BOUNDED',
    previewModel: 'COMBAT_DELTA',
    persistenceSchema: 'shardskin.verdict.v1',
    refinementHooks: [],
    playerFacingSummary:
      'Verdict. Premium source. When selected on a committed equipped ultimate, consumes all current Shards and Edge, resolves the native ultimate unchanged, then deals an Occult budget equal to 150% of the consumed total, split across the ultimate’s locked native targets. Gains 10 Shards afterward.',
  },
];

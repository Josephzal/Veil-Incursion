/** @deprecated Immersive mode is enabled globally in App.tsx via useImmersiveChrome. */
export function useImmersiveCombatChrome(_active = true): void {
  // no-op — kept for backward compatibility.
}
